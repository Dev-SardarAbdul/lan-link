import styled, { css } from "styled-components";

import { Card } from "react-bootstrap";

type Props = {};

export const MainCard = styled(Card)<Props>``;
export const MainImg = styled(Card.Img)<Props>``;

export const MainBody = styled(Card.Body)``;
export const MainTitle = styled(Card.Title)<Props>``;
export const MainText = styled(Card.Text)``;

export const IconImg = styled.img<Props>``;
